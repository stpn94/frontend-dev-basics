/**
 * unamed export (default)\
 *
 * 이름없는 객체 1개를 exports하는 것이다.
 * module.exports (CommonJS)
 *
 * */

export default {
  add: function (a, b) {
    return a + b;
  },
  substract: function (a, b) {
    return a - b;
  },
};
